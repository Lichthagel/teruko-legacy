mutation ($url: String!) {
    createImageFromUrl(url: $url) {
        id
        filename
        title
        source
        createdAt
        updatedAt
        tags {
            slug
        }
    }
}
