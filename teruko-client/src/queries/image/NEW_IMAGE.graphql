mutation ($files: [Upload!]!) {
    createImage(files: $files) {
        id
        filename
        title
        source
        createdAt
        updatedAt
        tags {
            slug
        }
    }
}
