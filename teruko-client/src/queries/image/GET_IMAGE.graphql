query Image($id: String!) {
    image(id: $id) {
        id
        filename
        title
        source
        createdAt
        updatedAt
        tags {
            slug
            category {
                color
            }
        }
    }
}
