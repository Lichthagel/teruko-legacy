query Image($id: Int!) {
    image(id: $id) {
        id
        filename
        title
        source
        createdAt
        updatedAt
        tags {
            slug
            category {
                color
            }
        }
    }
}
