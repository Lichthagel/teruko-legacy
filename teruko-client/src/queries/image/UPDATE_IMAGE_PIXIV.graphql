mutation UpdateImagePixiv($id: String!, $source: String) {
    updateImagePixiv(id: $id, source: $source) {
        id
        title
        updatedAt
        tags {
            slug
            category {
                color
            }
        }
    }
}
