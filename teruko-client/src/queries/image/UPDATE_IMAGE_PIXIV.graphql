mutation UpdateImagePixiv($id: Int!, $source: String) {
    updateImagePixiv(id: $id, source: $source) {
        id
        title
        updatedAt
        tags {
            slug
            category {
                color
            }
        }
    }
}
