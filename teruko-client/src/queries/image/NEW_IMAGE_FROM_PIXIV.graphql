mutation ($url: String!) {
    createImageFromPixiv(url: $url) {
        id
        filename
        title
        source
        createdAt
        updatedAt
        tags {
            slug
        }
    }
}
